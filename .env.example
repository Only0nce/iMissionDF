# Optional override for the protected network-administration password.
# Supply a 64-character SHA-256 hex digest through the process environment.
# Treat the real digest as sensitive; never commit it.
ISCAN_NETWORK_ADMIN_PASSWORD_SHA256=
